# Things To Do

* Navigate into table structure (cols, indices,..) in the tree.
* Avoid if possible it re-reading the code structure for each new task
* Get claude in container to use gopls upfront
* Show cell contents
* Easy (easier) add filter if a cell is selected
* Show FK the reverse way (somehow)
* Show running tasks (background queries running)
* Show a log of queries


